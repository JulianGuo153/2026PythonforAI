# Python 语法特性
## 支持多个赋值
print(a, b) \
a, b = b, a \
print(a, b)

## 缩进代替“{}”
## for循环

for x in list1: \
&emsp;&emsp;print(x) \
通过标签在一个列表中遍历 \
遍历字典是遍历字典的键值 \
range()的范围是左闭右开，list[]的范围是左闭右开 \
使用for循环表达式可以简化代码 \

## 缺省参数与位置参数
缺省参数必须在位置参数的后面 \

## 类的继承关系
子类继承多个父类时，遍历同名方法的顺序遵循子类的MRO顺序 \
person.\_\_mor\_\_&ensp;或&ensp;person.mor() \
一般是“子类优先，从左到右”

## 引入模块
"import numpy as np"将numpy模块记为类np

## 矩阵的合并
"array3 = np.concatenate((array, array2), axis=0)" \
axis=0 是一个隐含参数，一般合并在第axis+1维上，默认值为0

## 矩阵的切片
print(list1[1:3]) \
print(array[1:3]) \
print(array[1:3, 1:3]) 

理论上矩阵的维数有多少，矩阵的切片也可以是多少维 \
矩阵也可以跳着切片 

## Pytorch的张量tensor
tesonr求解梯度需要方法tensor.backward() \
tensor的允许用梯度需要用方法tensor.require_grad(Ture)\
tensor上的计算了梯度的数据需要用tensor.detach().numpy()取下 \
tensor的梯度会累计，不需要累积时应当清零，如x.grad=torch.tensor(0.0) \
除了用列表产生张量，也可以用 torch.ones()、torch.zeros()和torch.normal(0, 1, (3, 10, 4))创建 \
张量加法 torch.sum(tensor, dim=1, keepdim=True) \
张量的形状 roech.shape \
